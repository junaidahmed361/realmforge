# Realm schema mapping template

Map your domain fields to backbone concepts:
- Observed variables (o_t)
- Latent targets (z_t proxies)
- Actions (a_t)
- Outcomes (y_t)
- Factor constraints (E_i)

Also document missingness, temporal granularity, and leakage boundaries.
